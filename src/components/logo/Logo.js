import React from 'react';
import {Image} from 'react-native';
import LogoIcon from '../../assets/images/logo.png';

const Logo = () => <Image style={{width: 80, height: 80}} source={LogoIcon} />;

export default Logo;
